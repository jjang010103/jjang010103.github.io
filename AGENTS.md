# 포트폴리오 개발 및 작성 원칙 (AGENTS.md)

이 저장소의 **단일 규칙 문서**다. 콘텐츠 원칙과 기술 규칙을 모두 여기에 둔다.
`CLAUDE.md`는 이 파일을 불러오는 포인터일 뿐이므로, 규칙을 더하거나 고칠 때는 **항상 이 파일을 고친다.**

## 0. 저장소 구성

| 파일 | 성격 |
| --- | --- |
| `index.html` | 사이트 전부(템플릿·스타일·데이터·로직). 거의 모든 수정은 여기서 한다. |
| `support.js` | **생성 파일, 수정 금지.** dc-runtime 번들. React를 CDN에서 UMD로 받아 문서를 렌더한다. |
| `AGENTS.md` | 이 문서. |
| `CLAUDE.md` | 이 문서를 가리키는 포인터. |
| `.document/History/*.md` | 과거 구조 결정 기록(ADR). 이미 검토하고 기각한 "당연해 보이는 개선"이 여기 적혀 있다. 큰 구조를 되돌리기 전에 먼저 읽는다. |
| `DESIGN.md` | 시각 체계 명세(토큰 frontmatter + 색·타이포·레이아웃·컴포넌트 규칙). 새 화면·컴포넌트를 그릴 때 먼저 읽는다. 토큰 값의 원본은 `index.html`의 `:root`이고, 이 파일은 그 값을 옮겨 적은 것이라 **토큰을 바꾸면 여기도 갱신한다**(`/impeccable document` 재실행). |
| `.impeccable/design.json` | `DESIGN.md`의 사이드카(그림자·모션·컴포넌트 스니펫). 손으로 고치지 않고 `DESIGN.md`와 함께 재생성한다. |

빌드 단계·package.json·테스트·린터가 없다. 완전한 정적 사이트다.

---

## 1. 핵심 철학과 추구미 (Core Philosophy)

### ① 시인성 (Readability & Scannability)
- **짧은 시선 이동**: 방문자가 긴 줄글을 읽지 않고도, 짧은 시선 이동만으로 문단 하나가 단번에 이해되어야 한다.
- **불릿 & 볼드 키워드 중심**: 줄글(벽돌글)을 배제하고, [키워드/라벨] + 볼드 강조를 활용해 0.5초 만에 스캔 가능한 구조를 유지한다.
- **자연스러운 인지 흐름 (순서의 중요성)**:
  - 마우스 휠로 위아래를 왔다 갔다 하지 않고, 위에서 아래로 물 흐르듯 읽혀야 한다.
  - 이게 뭔지(개요)도 모르는데 성과나 기술부터 들이밀지 않는다.

### ② 독자 타겟팅 (HR & Technical Interviewers)
- **1차 독자는 비개발자(인사팀)**: 거창하거나 난해한 번역투 전문용어를 지양하고, 비개발자도 직관적으로 이해할 수 있는 쉬운 한국어 어휘를 우선 사용한다.
- **2차 독자는 기술 면접관**: 쉬운 어휘로 흐름을 잡되, 세부 내용에는 명확한 기술 스택·최적화 기법·정량 수치를 담아 전문성을 증명한다.

### ③ 금지 표현 (쓰지 말 것 → 대신 이렇게)

| 쓰지 않는다 | 이유 | 대신 |
| --- | --- | --- |
| 구현 판단, 아키텍처 레벨 의사결정 | 번역투·모호함 | 설계, 구조를 분리, 방식을 정함 |
| 사내 표준화 및 기여 | 명사 나열, 무엇을 했는지 안 보임 | 전사 표준으로 확립, 공통 모듈로 분리 |
| 고도화, 개선 작업 진행, 최적화 수행 | 무엇을 얼마나 했는지 없음 | 무엇을 어떻게 → **수치** (예: 바이너리 50% 감축) |
| 완벽한, 혁신적인, 최고의, 최첨단 | 근거 없는 과장 | 수치나 사실로 대체하거나 삭제 |
| leverage / utilize / synergy 직역체 | 영문 이력서에서도 상투어 | Built, Led, Reduced, Migrated 등 동작 동사 |
| ~을 담당하였음, ~하도록 하였습니다 | 늘어지는 문어체 | ~주도, ~구축, ~개선 (동사 종결) |

### ④ 분량 상한 (넘기면 시인성이 깨진다)

- `blurb`(상세 히어로 부제): **1~2문장**, 60~90자. 시스템의 성격 + 내 역할까지만.
- `summary`(프로젝트 카드 한 줄 요약): **한 문장**, 30~40자. `intro`를 줄인 것이고 새 사실을 넣지 않는다. 1칸 카드에서 3줄 안에 들어가야 한다(2026-09-25 추가).
- `intro`(상세 01 섹션 본문): **1~2문장**, 60~85자. `blurb`와 같은 말을 반복하지 않는다 — 상세 페이지에서 히어로 바로 아래 01 카드로 이어져 보인다.
- `features` / `metrics` / `challenges` / `ai`: 섹션당 **1~2개.** 3개를 넘기면 합치거나 뺀다.
- 한 항목: **한 문장.** 60자 안팎을 노리고, **90자를 넘기면 쪼갠다.**
- 볼드 강조(`**…**`): **한 항목에 최대 1개**, 없어도 된다. 전 항목을 볼드로 칠하면 강조가 사라진다.
- `stats` 카드: 프로젝트당 **2~3개.** 값(`v`)은 짧게, 라벨(`k`)은 단위·대상만.
- `stack` 태그: **3~7개.** 나열이 길어지면 대표 기술만 남긴다.

> 위 수치는 감이 아니라 현재 `PROJECTS` 실측값이다(2026-09-03 기준: 섹션 항목 1~2개·총 29개, 항목 43~82자, `blurb` 79~84자, `intro` 56~77자, `stats` 4개 프로젝트에 10장, `stack` 3~7개).
> **기존 항목이 이 범위 안에 있으면 손대지 않는다.** 상한은 새로 쓸 때의 기준이지, 리팩터링 지시가 아니다.

---

## 2. 프로젝트 상세 페이지 표준 구조 및 순서 (5단계)

모든 프로젝트의 상세 정보는 방문자의 인지 흐름에 맞춰 반드시 아래 **5단계 순서**를 따른다.

1. **프로젝트 개요 및 목표** — 시스템의 성격과 시작 배경/목표 (1~2줄로 명확하고 쉽게)
2. **주요 개발 내용** — 본인이 직접 설계하고 구현한 핵심 기능 및 아키텍처
3. **주요 성과** — 결과 및 정량 수치 지표 (예: ezZip DAU 6만, 바이너리 50% 감축, iSend 일 90만 건)
4. **문제 해결 및 최적화** — 마주친 기술적 난관(성능·메모리·용량·데이터 정합성)과 극복한 해결책
5. **AI 활용** — AI 도구로 생산성·품질을 끌어올린 경험.
   **현재 이 항목이 있는 건 ezZip 하나뿐이다.** 나머지 5개는 `ai: []`로 비어 있고, 사용자가 내용을 주기 전까지 그대로 둔다 — 순서를 채우려고 지어내지 않는다(§4).

**데이터 구조와의 대응** — 5단계는 각각 별도 필드이고, 상세 페이지의 본문 카드마다 **번호(01~05)와 소제목**이 한 줄로 드러난다.

| 단계 | 필드 | 상세 페이지 표현 |
| --- | --- | --- |
| 01 | `intro` (없으면 `blurb`) | 전체 폭 카드, 문단 |
| 02 | `features` | 전체 폭 카드, 불릿 |
| 03 | `metrics` + `stats` | 전체 폭 카드, 불릿. `stats`는 본문 위 **수치 카드 줄** |
| 04 | `challenges` | 전체 폭 카드, 불릿 |
| 05 | `ai` | 전체 폭 카드, 불릿 (번호와 제목을 `--accent-line`). 소제목 "AI 활용" |

- 소제목 문구는 `T` 상수(`T.intro` … `T.ai`)에 3개 국어로 있다. 프로젝트마다 새로 쓰지 않는다.
- **번호는 T5 배열 인덱스로 고정**된다. 빈 섹션은 렌더에서 빠지지만 뒤 번호가 당겨지지 않는다 — `features`를 비우면 01 다음이 03이 되어 번호가 튄다. 현재 비는 건 `ai`뿐이라 대부분 04에서 끝난다. 번호 레일(세로선)은 2026-09-03 리디자인에서 가로 배치로 바뀌었다.
- `stats`는 **`metrics` 문장에 이미 있는 수치만** 카드로 뽑는다(§4). 수치가 아닌 성과(Zero Leak, FCM 등)는 카드로 만들지 않는다 — `sc-if`가 카드 줄을 통째로 숨긴다.
- 상세 렌더 순서: "← 메인으로" + 페이저(중간 폭만 — 데스크톱은 켜진 "프로젝트" 칩 안으로 페이저가 들어가고 이름이 "메인으로" 역할, 모바일은 위 바 맨 앞 ←만 있고 넘기기는 없다) → 히어로 벤토(액센트 카드 `프로젝트 nn · co`(T.project, 3개 국어) / `mtitle` / `blurb`, 기간 카드, 기술 스택 카드, "결과물 보기" 잉크 카드 — `url`이 없으면 내용 없는 잉크 칸) → 대표 이미지 → `stats` 수치 카드 → 01~05 본문 카드 → 메인과 같은 푸터(푸터는 두 뷰 밖에 한 벌만 있다). 하단 "다음 프로젝트" 카드는 없앴다(2026-09-25). 인원·형태는 데이터에 필드가 생길 때만 기간 카드에 넣는다.

---

## 3. 메인 페이지 원칙

- **맥락 없는 특정 프로젝트 지표 배제**: 방문자가 프로젝트를 알기도 전에 프로필 상단에 특정 제품의 세부 KPI(예: ezZip DAU 6만 등)를 노출하지 않는다. 제품별 수치는 해당 프로젝트 상세 페이지에서 맥락과 함께 제시한다.
- **간결하고 담백한 연결**: 프로필 벤토(히어로 3×2 · 총 경력 · 연락처 · 기술 스택 전체 폭)에서 경력 카드로 이어진다. 별도 About·스택 섹션은 없다. 프로필 사진 칸·구직 상태 카드·CTA 문구는 시안에서 뺐다(2026-09-25).
- **연락처는 텍스트 + 복사 버튼이다.** 이메일·전화번호는 그대로 표시하되 링크를 걸지 않는다. 복사 아이콘 버튼만 동작하고 결과는 `aria-live`로 알린다. 연락처 잉크 카드·푸터 모두 이메일·전화 하이퍼링크 금지(사용자 결정, 2026-09-24). GitHub·LinkedIn만 링크다.
- **프로젝트 카드의 이미지 칸은 비어 있어도 텍스트로 메우지 않는다.** 대표(2×2)는 위, 가로형(2칸)은 왼쪽에 이미지 자리가 있고 `PROJECTS[].img`를 채우면 켜진다. 1칸 보조 카드에는 이미지가 없다. 상세의 대표 이미지 칸도 같은 규칙이다.
- **총 경력은 하드코딩하지 않는다.** `CO[].period`(양 끝 달 포함)의 개월 합에서 채운 햇수 + 1을 "N년차"로 그린다. 프로필 총 경력 카드와 푸터 역할 줄이 같은 값이다. 히어로 문장 속 "8년차"는 기존 문구라 고정이다 — 어긋나면 문구를 사용자에게 확인한다.
- **프로젝트 카드에는 성과를 노출하지 않는다.** 기간·제목·`summary` 한 줄·기술 태그(`stack`)만 표시한다. 수치 카드와 성과 문구는 상세 페이지에서만 표시한다(사용자 결정, 2026-09-24).
- **기술 스택은 프로필 벤토의 전체 폭 카드 하나다.** 카드 라벨은 화면에서 빼고(`vh`) 첫 묶음 "핵심 스택"이 라벨 역할을 한다(2026-09-26). `STACK` 네 묶음을 4열로 두고, 핵심 스택(첫 묶음)만 15px 알약으로 강조하며 나머지는 알약 없는 15px 글자다. 전부 알약으로 만들면 카드가 쓸데없이 높아진다(사용자 지적, 2026-09-25). 표로 나열하지 않는다. 2026-09-24의 "칩 금지" 결정은 DESIGN.md 2026-09-25 개정이 대체했다.
- 섹션 순서는 `NAV` 상수가 정의한다: 프로필 → 경력 → 프로젝트 → 학력·자격증(4개). 상세 뷰에서는 "프로젝트"가 켜진다. 섹션 머리는 제목만 둔다 — `02 / CAREER` 같은 번호 라벨은 의미가 없어 뺐다(사용자 결정, 2026-09-25). 순서를 바꾸면 내비의 활성 챕터 표시도 함께 검증한다.

---

## 4. 성과 수치 규칙 (사실성)

이력서다. 수치는 검증 가능해야 한다.

- **사용자가 제공한 수치만 쓴다.** 에이전트가 새 수치를 추정·생성·반올림 변형해 만들어내지 않는다.
- 문서에 없는 수치가 필요하면 **지어내지 말고 물어본다.** 비워두는 편이 틀린 숫자보다 낫다.
- 기존 수치의 단위·표기 정리(`60K` ↔ `6만`)도 임의로 바꾸지 않는다. 언어별 관례상 필요한 경우에만, 값은 그대로 두고 표기만 맞춘다.
- 3개 국어의 같은 항목은 **같은 숫자**를 담아야 한다. 한쪽만 고치는 실수가 가장 잦다.

---

## 5. 다국어(ko / en / ja) 규칙

### 작성 톤
- **현지 이력서 관용 표현으로 의역한다.** 한국어 문장 구조를 그대로 옮기지 않는다.
  - 영어: 동작 동사로 시작하는 성과 중심 불릿 (`Led`, `Built`, `Reduced`, `Re-engineered`).
  - 일본어: 일본 이력서/職務経歴書 어투. 체언 종결(`〜を構築`, `〜を達成`) 중심.
- 의미와 수치는 동일하게, 문장 구조는 달라져도 된다.

### 구현 방식 (두 가지, 섞어 쓰지 않는다)
- **템플릿**: 형제 `<span data-l="ko|en|ja">`(인라인) 또는 `data-lb`(블록). `html[lang]`에 걸린 CSS로만 표시/숨김이 결정되므로 언어 전환 시 리렌더가 없다. 한 언어를 추가한다는 건 **세 형제를 모두 추가**한다는 뜻이다.
- **스크립트**: `{ ko, en, ja }` 객체를 `renderVals()` 안의 `t(o)`로 해석한다. 회사명은 반드시 `CO` 상수를 거쳐 표기를 통일한다.
- 어느 쪽이든 **세 언어가 모두 채워지지 않은 상태로 커밋하지 않는다.**
- **보이지 않는 글도 같은 규칙이다.** `aria-label`, `title`, 라이브 리전 문구는 `T` 상수(`T.toDark`, `T.pdf` …)와 `CONTACTS[].label / done`에서 온다. 템플릿에 `aria-label="닫기"`처럼 한 언어를 박지 않는다 — 스크린리더는 그 언어로 읽는다. 언어 버튼의 `aria-label="한국어 / English / 日本語"`만 각 언어 자기 표기라 고정값이다.

### 줄바꿈은 언어에만 건다
`word-break:keep-all`은 `html[lang="ko"] body` 한 곳에만 있다. 컴포넌트 규칙에 직접 쓰면 일본어에 상속되어 어절 경계가 없는 문장이 통째로 넘친다(2026-09-05 감사: 모바일 가로 오버플로 294px, 그 때문에 레이아웃 뷰포트가 669px로 늘어나 바텀시트가 화면 밖에 그려졌다). 안전망은 `body{overflow-wrap:break-word}`, 일본어는 `html[lang="ja"] body{line-break:strict}`.

---

## 6. 프로젝트 추가·수정 체크리스트

`PROJECTS`에 항목을 넣거나 고칠 때 순서대로 확인한다.

1. `id`가 고유한가. (상세 상태 `openId`와 해시 `#/p/<id>`가 이 값을 쓴다. `[\w-]`만 쓴다)
2. `title` / `mtitle` / `blurb` / `summary` / `intro` / 각 섹션 항목 / `stats`의 `v`·`k` 전부 **ko·en·ja 3개**가 있는가.
3. 내용이 `features`(02) → `metrics`(03) → `challenges`(04) → `ai`(05) 중 **맞는 필드**에 들어갔는가. 없는 단계는 배열을 비워 둔다(§2).
4. 섹션당 1~2개, 한 항목 한 문장(90자 이내), 볼드 최대 1개 — §1-④ 분량 상한을 지켰는가.
5. 수치가 전부 사용자 제공값인가. `stats`의 값이 `metrics` 문장에 실제로 있는가. 3개 국어의 숫자가 같은가.
6. `date` 표기가 기존과 같은 형식(`2025.02 — NOW`)인가. `NOW`는 그대로 쓴다 — 렌더 시 `T.now`로 언어별(현재 / NOW / 現在) 치환된다.
7. `url`이 없으면 빈 문자열 `''`. (`sc-if`가 링크 줄을 숨긴다)
8. 브라우저에서 카드 → 상세 페이지 → 3개 언어 → 라이트/다크 → 720px → **인쇄 미리보기**까지 확인했는가.
9. 새로 넣은 항목의 `span / rows / ink`가 벤토를 빈칸 없이 채우는가. `CARD_ORDER`에 id를 넣었는가. 강조(`ink`) 카드는 최대 둘인가.

---

## 7. 기술 아키텍처

### 실행

```bash
powershell -NoProfile -ExecutionPolicy Bypass -File .claude/server.ps1
# → http://localhost:8734
```

`server.ps1`은 HttpListener 한 개짜리 정적 서버이고 `Cache-Control: no-store`를 붙인다(하드 리로드 없이 바로 반영). 루트는 `$PSScriptRoot`의 상위이므로 저장소를 옮겨도 따라온다. `.claude/launch.json`이 같은 서버를 8734 포트의 `portfolio` 프리뷰로 등록하지만 그쪽 `-File` 경로는 절대 경로다 — 저장소를 옮기면 함께 고친다.

빌드 단계가 없으므로 `index.html`을 파일로 직접 열어도 동작한다. 다른 정적 서버를 써도 무방하다.

인쇄/PDF 출력은 **부가 기능이 아니라 1급 기능**이다(`@media print` 블록 + PDF 버튼). 레이아웃을 건드리면 반드시 인쇄 미리보기까지 확인한다(`Ctrl+P`). 잉크·액센트 카드(히어로·연락처·잉크 프로젝트 카드·푸터·상세 히어로·결과물 카드)는 `print-color-adjust:exact`로 배경을 강제하고, 흰 카드는 종이에서 사라지지 않게 헤어라인을 두른다 — 없으면 브라우저 기본값이 배경만 버려 밝은 글자가 종이색에 묻힌다.

### index.html 구조 (약 1400줄, 4개 구역)
1. **실제 `<head>`** — 첫 페인트 전에 저장된 테마/언어와 현지화된 `<title>`을 적용하는 인라인 부트 스크립트. `<helmet>`이 아니라 `<head>`에 있어야 한다(런타임이 helmet을 관리 대상으로 삼아 매 렌더마다 `<title>`을 되돌린다). 옮기기 전에 그 자리의 주석을 읽는다.
2. **`<helmet data-dc-atomics>`** — meta·폰트 링크, 그리고 **유일한 전역 `<style>` 블록**.
3. **템플릿** (`</x-dc>`까지) — `{{ binding }}` 보간과 `<sc-for list="{{ … }}" as="x">` 루프. 레이아웃 훅은 `data-*` 속성(`data-bar`, `data-main`, `data-detail`, `data-projgrid`, `data-noprint` …)이고 스타일시트는 이것을 타깃한다.
4. **`<script type="text/x-dc" data-dc-script>`** — 데이터 상수(`STACK`, `EDU`, `CO`, `PROJECTS`, `NAV`, `T`), `bold()` 헬퍼, `class Component extends DCLogic`.

`renderVals()`가 반환하는 객체의 키가 템플릿의 `{{ 바인딩 }}`이다. 상태는 `lang`, `theme`, `openId`, `copied`, `navIdx`, `scrolled`(데스크톱 바 뒤 흐림·모바일 알약 전환), `menuOpen`(모바일 메뉴)이다. 메인(`[data-main]`)과 상세(`[data-detail]`)는 같은 문서의 두 뷰이고 `openId`가 어느 쪽을 보일지 정한다. `activeChapter()`가 sticky 바의 아랫변 기준으로 활성 챕터 하나를 고르고, 인덱스가 바뀐 프레임에만 `switchChapter()`가 setState한다. 켜진 챕터는 `is-on` 클래스의 잉크 알약이다(2026-09-25에 늘어나는 잉크·`navLayout()` 제거). `<html>`에 건 `ResizeObserver`가 폰트 로드·리플로 후 다시 측정한다. 스크롤·리사이즈·옵저버는 전부 `onScroll`로 들어와 `requestAnimationFrame` 한 번으로 합쳐진 뒤 `measure()`가 setState한다 — 프레임당 렌더 한 번.

**프로젝트 상세는 해시 라우트다(2026-09-25, 모달 대체).** `#/p/<id>`가 유일한 진입점이고 `applyHash()`가 `hashchange`·첫 로드에서 뷰를 맞춘다 — 새로고침·뒤로가기·직접 링크가 모두 여기를 지난다. `open()`은 스크롤 위치를 저장하고 해시를 쌓는다(`location.hash`). 상세 안의 페이저·좌우 화살표 키(`step(±1)`, 끝에서 멈춤)는 `location.replace`라 기록을 쌓지 않는다 — 뒤로가기 한 번이면 메인이다. 닫기("← 메인으로"·Esc)는 우리가 쌓은 기록이면 `history.back()`, 해시로 바로 들어왔으면 `replaceState`로 해시만 지운다(`closeDetail`). 열면 맨 위로 올리고 `h1#dt-title`(tabindex -1)에 포커스, 페이저로 넘길 때는 누른 버튼에 포커스를 두고 끝에 닿아 비활성이 되면 `h1`로 옮긴다. 닫으면 저장한 스크롤 위치로 돌아가 마지막으로 본 프로젝트의 `.pc-btn`에 포커스한다. 상세에서 챕터를 누르면 메인으로 나와 그 섹션으로 간다. 이름을 누르면 "← 메인으로"와 같이 보던 카드 자리로 돌아간다(사용자 결정, 2026-09-26). Alt/Ctrl/Meta가 눌린 화살표는 브라우저 몫이라 무시한다. 모달 시절의 `inert`·포커스 트랩·스와이프 닫기·바텀시트·`body` 스크롤 잠금은 모두 없다. 카드는 `<article>`이 클릭 면이고 접근성 컨트롤은 `h3 > button.pc-btn` 하나다 — article에 `role=button`을 주면 h3가 버튼 이름에 묻혀 헤딩 탐색에서 사라진다.

`DCLogic`은 support.js가 전역으로 제공한다. 스크립트 안에서 `React.createElement`를 쓸 수 있다(`bold()` 참조).

### 스타일 — 스타일시트가 유일한 소스
2026-09-02 리팩터링에서 정적 인라인 `style=` 약 255개와 `!important` 152개를 전부 걷어냈다. "데스크톱을 고치면 모바일이 깨지던" 원인이 그 이중 구조였기 때문이다. **컴포넌트의 크기·색·배치 명세는 `DESIGN.md`에 있다.** 여기에는 구조 규칙만 둔다:

- 정적 스타일은 `<style>` 블록에 클래스 또는 `data-*` 규칙으로 넣는다. 인라인 `style=`은 3개뿐이고 **전부 `{{ }}` 동적 바인딩**이다(메인 표시 `display`, 테마 아이콘 회전, 프로젝트 `--span/--rows`). JS가 만드는 요소(`bold()`의 `<strong>`)에도 인라인 스타일을 주지 않는다. **예외를 만들지 않는다.**
- 데스크톱은 `@media screen and (min-width:1001px){.page{zoom:.9}}` 한 줄로 0.9배로 그린다(사용자 결정, 2026-09-25). 밀도를 바꾸고 싶으면 개별 px가 아니라 이 값을 고친다.
- 데스크톱(4열 벤토) 값이 기본이고, **모바일 재정의는 시트 하단의 `@media (max-width:720px)` 블록 하나에서만** 한다. 721~1000px 중간 폭 블록은 벤토를 2열로 줄이는 용도다. 두 번째 모바일 블록을 만들지 않는다. `!important`도 쓰지 않는다 — 이길 인라인이 없으므로 필요가 없다. 모바일 블록에 기본값과 같은 값을 다시 적지 않는다.
- 색은 `:root` 토큰(이름은 DESIGN.md colors와 1:1)에서만 온다. 새 색이 필요하면 **토큰부터 추가**하고, 헥스값을 직접 박지 않는다. `light-dark()`는 2024년부터 모든 주요 브라우저가 지원하므로 구형 브라우저용 폴백 블록은 두지 않는다(2026-09-25 제거).
- **글자색 토큰은 놓이는 면 기준 4.5:1을 지킨다.** 흰 카드·쟁반·`--media`·잉크·액센트 다섯 면 위에서 잰다(2026-09-05 감사에서 카드 위 3.1:1이 잡혔다). 면 전용 색(`--accent`)과 글자·선 전용 색(`--accent-line`)을 나눈 이유가 이것이다.
- 모바일 벤토는 1열 나열이 아니라 **반반 섞기**다: 글이 긴 카드는 전체 폭, 짧은 카드만 반씩(DESIGN.md). 배율(zoom)로 줄여 여러 열을 억지로 넣는 안은 사용자가 되돌렸다(2026-09-26). 반 칸 카드는 390px에서 3개 언어로 넘침을 잰다.
- 모바일에서 `[data-bar]`는 화면 위 바(언어 · 테마 · 메뉴 가운데 정렬, 이름 없음 — 맨 위에서는 바탕 없는 전체 폭, 스크롤하거나 메뉴를 열면 흰 알약 `is-pill`)가 되고, 챕터는 데스크톱과 **같은 마크업**을 `[data-bar].is-open`일 때 버튼 줄 아래로 펼친다(상태 `menuOpen`, 2026-09-26). 모바일 전용 메뉴 마크업을 따로 만들지 않는다(메뉴의 PDF 버튼과 바깥 터치 층만 예외). 2026-09-05의 "본문을 가리는 우하단 고정 배치 금지"는 이 알약에 한해 사용자가 뒤집었다.
- 손가락 타깃은 모바일에서 44px다(DESIGN.md). 2026-09-05에 사용자가 36px로 되돌린 적이 있으니 바가 무겁다는 지적이 다시 나오면 그 기록부터 본다.
- 같은 마크업을 두 번 쓰지 않는다. 연락처(이메일·전화)는 `CONTACTS` 한 목록을 프로필 카드와 푸터가 `sc-for`로 돈다.
- 구조 의존 셀렉터(`:first-of-type`, `> div:first-child`, 속성 부분일치)를 쓰지 않는다. 전부 취약해서 제거했다 — 명시적인 `data-*` 훅을 잡는다.

### 테마 (라이트/다크)
CSS `light-dark()`와 `<html>`의 `colorScheme`으로만 처리한다. **한쪽 테마 전용 색을 하드코딩하지 않는다.** 토글은 View Transition 원형 확산(`data-vt`, `--vtx/--vty/--vtr`)으로 애니메이션한다. 테마·언어는 localStorage에 `mj-resume-theme` / `mj-resume-lang`으로 저장되고, 첫 페인트 전에 `<head>` 부트 스크립트가 복원한다.

JS가 색을 만드는 곳은 없다. 언어 버튼은 상단 바의 `.icon-btn.is-on` 한 벌이고, `renderVals()`의 `cls`가 주는 `is-on` 클래스로 CSS가 칠한다(2026-09-05 critique 수정). `dark ? '#…' : '#…'` 리터럴 쌍을 다시 만들지 않는다.

### 강조 표기
`PROJECTS[].items` 문자열 안의 `**…**`는 `bold()`가 렌더 시 `<strong>`으로 바꾸고, CSS가 배경 없는 굵은 글씨로 그린다. 눈에 걸려야 할 키워드/수치 하나에만 쓴다.

### 커밋
한국어 메시지 + 컨벤셔널 커밋 접두사(`style:`, `feat:`, `refactor:` …). 기존 히스토리와 동일하게.

---

## 8. 검증

이 저장소에는 빌드·테스트·린터가 없다. 그래서 **아래 grep이 유일한 자동 검증 수단**이다. 판단이 필요한 것(문체·시인성)보다 먼저, 기계가 답할 수 있는 것부터 확인한다.

### ① 기계가 확인하는 것 — 고치기 전과 후에 돌린다

```bash
# 1. 3개 국어 짝이 맞는가 (템플릿) — ko/en/ja 개수가 서로 같아야 한다
awk '/<\/style>/,/<\/x-dc>/' index.html | grep -o 'data-lb\?="[a-z][a-z]"' | sort | uniq -c

# 2. 3개 국어 짝이 맞는가 (스크립트) — 세 숫자가 같아야 한다
awk '/data-dc-script/,0' index.html | grep -oE "(ko|en|ja): ['\"]" | sed 's/:.*//' | sort | uniq -c

# 3. !important 0, 모바일 미디어 블록 1, 취약 셀렉터 0
grep -c '!important[;}]' index.html
grep -c '@media (max-width:720px){' index.html
grep -cE ':first-of-type|:last-of-type|> *div:first-child|style\*=' index.html
```

**2026-09-26 기준 통과값:** 1 → `data-l` 20/20/20 · `data-lb` 11/11/11 | 2 → 128/128/128 | 3 → `0`, `1`, `0`.

4번째 검증 — **시트에 다크 리터럴이 남지 않았는가.** 색은 토큰에서만 온다(§7). 예외는 없다.

```bash
awk '/<style>/,/<\/style>/' index.html | grep -vE '^ *--|^ *:root\{' | grep -oE 'light-dark\(#[0-9a-f]{3,6},#[0-9a-f]{3,6}\)' | sort | uniq -c
```
→ **출력이 없어야 한다**(2026-09-25). 선택 색상도 `--ink` / `--surface` 토큰으로 옮겨 예외가 사라졌다.

숫자가 어긋나면 **거기서 멈춘다.** 1·2번은 한 언어만 고치고 커밋하는 사고를, 3·4번은 걷어낸 구조(인라인 스타일·하드코딩 색)가 되살아나는 것을 잡는다.

### ② 사람이 확인하는 것 — 자동화할 수 없다

레이아웃이나 텍스트를 건드렸으면 서버를 띄우고(§7) 전부 본다. 하나라도 건너뛰면 확인한 게 아니다.

- 3개 언어 전환 — 잘린 문장, 깨진 줄바꿈. **모바일 폭에서도** 세 언어를 본다(일본어는 데스크톱에서 멀쩡해도 375px에서 넘칠 수 있다)
- 라이트/다크 토글 — 한쪽 테마에서만 안 보이는 색
- 720px 이하 — 상세 페이지, 프로젝트 그리드, 상단 바
- 키보드만으로 카드 → 상세(제목 포커스) → 이전·다음·좌우 화살표 → Esc → 카드로 복귀. 해시(`#/p/<id>`)로 바로 들어오기·새로고침·뒤로가기
- **인쇄 미리보기(`Ctrl+P`)** — 1급 기능이다
- 인쇄: 메인은 1열, 상세가 열려 있으면 그 프로젝트만

### ③ 이 저장소에 적용하지 않는 것

에이전트 하네스 논의에 나오는 실행 루프·권한 경계·비용 예산·트레이싱은 **무인 실행되는 에이전트를 위한 장치**다. 이 저장소는 사람이 보면서 고치는 정적 페이지 하나이고, 외부로 나가는 부수효과도 배포도 없다. 그런 계층을 여기 만들지 않는다 — 유일한 권한 규칙은 §0의 **`support.js` 수정 금지**다.

---

## 9. 안티패턴 (실제로 났던 사고)

새 규칙은 취향이 아니라 사고에서 나온다. 되풀이하지 않기 위해 기록한다.

| 날짜 | 커밋 | 무엇이 문제였나 | 지금 규칙 |
| --- | --- | --- | --- |
| 2026-09-02 | `4c15373` | 인라인 `style=` 255개와 `!important` 152개가 싸워서, 데스크톱을 고치면 모바일이 깨지고 모바일을 고치면 데스크톱이 깨졌다 | §7 스타일 — 스타일시트가 유일한 소스 |
| 2026-09-02 | `4c15373` | 다국어를 렌더 바인딩으로 처리해 렌더당 동적 바인딩이 87개였다 | §5 구현 방식 — CSS 정적 표시 |
| 2026-09-03 | `cdf8b57` | 분량 상한을 실측 없이 적어 기존 항목과 모순됐다. 규칙을 지키려는 에이전트가 멀쩡한 콘텐츠를 고치게 만든다 | §1-④ 실측값 병기 |
| 2026-09-05 | 감사 수정 | `word-break:keep-all`을 컴포넌트 9곳에 직접 써서 일본어 모바일이 가로로 294px 넘치고 바텀시트가 화면 밖에 그려졌다. 한국어·영어만 보고 통과시켰다 | §5 줄바꿈은 언어에만, §8-② 3개 언어 전환은 모바일에서도 |
| 2026-09-05 | 감사 수정 | `aria-label="닫기"` 한국어 고정, 테마·복사 라벨 영어 고정. 보이는 글만 3개 국어였다 | §5 보이지 않는 글도 같은 규칙 |

배경은 `.document/History/`의 ADR에 있다.

---

## 10. 규칙을 바꾸는 방법

**같은 지적이 세 번 나오면 규칙으로 만든다.** 대화에서 매번 고쳐주는 것은 다음 세션에 남지 않는다. 승격 순서:

1. 대화에서 지적 → 그 자리만 고쳐진다
2. **이 문서의 규칙** → 이후 전부에 적용된다
3. **§8의 검증 명령** → 어겼는지 기계가 알려준다 ← 가능하면 여기까지 올린다

**규칙을 추가하기 전에**: §8 명령이 이미 잡아주는 내용인지 본다. 잡아준다면 산문 규칙은 넣지 않는다(있다면 지운다). 검증이 되는 것을 글로 또 적으면 문서만 길어지고 아무도 안 읽는다.

**구조를 바꿨으면**: 레이아웃 아키텍처·런타임 스택·정보 순서를 바꿨다면 `.document/History/`에 ADR을 추가하거나 갱신한다(한국어, 기존 형식: 맥락 / 결정 / 근거 / 결과). 다음 세션이 같은 논의를 다시 하지 않도록.

**수치가 바뀌었으면**: §1-④와 §8의 통과값은 실측치다. 콘텐츠나 구조를 늘렸다면 명령을 다시 돌려 숫자와 날짜를 갱신한다.

### 2026-09-25 벤토 개편 (시안 C + C-4)
DESIGN.md "한 판의 벤토"로 사이트 전체를 옮겼고, 프로젝트 모달을 해시 라우트 상세 페이지로 바꿨다. 2026-09-24의 프로젝트 1열 목록·스택 칩 금지는 이 개편이 대체했다(`.document/History/2026-09-25-bento-tray.md`).
